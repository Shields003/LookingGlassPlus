import React from "react";
import { Card } from "react-bootstrap";

//arrow function
const BigChart = () => {
  return (
    <Card className="bigchart">
      <h1>One big 'ol chart to rule them all!</h1>
      <h3>...and in the darkness bind them</h3>
    </Card>
  );
};

export default BigChart;